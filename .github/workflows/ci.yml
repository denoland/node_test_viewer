name: ci
on:
  push:
    branches: main
  pull_request:
    branches: main
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: denoland/setup-deno@v2
      - run: deno task check
